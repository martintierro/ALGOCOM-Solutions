import heapq
def solve(n,m,num):
    queue = []
    heapq.heapify(queue)    
    num = str(num)
    while(k>0):
        for i in range(n-k):
            heapq.heappush(queue, str)

n,k = list(map(int,input().rstrip().split(" ")))
num = input().rstrip()
print(solve(n,m,num))